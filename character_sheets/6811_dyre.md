# Dyre — character 6811

> Transcribed as-is from the history files. Nothing invented; values below are the exact tokens from the source block.
> Source: `characters/norse.txt` (line 4068), block `6811 = { ... }`.
> Display only: line endings normalized (CRLF → LF), encoding converted (Windows-1252 → UTF-8). Inline `#` comments are preserved in the verbatim sections.

## Identity (as in file)

| Field | Value in file |
|---|---|
| Character ID | `6811` |
| Name | `"Dyre"` |
| Sex | male (default — no `female=` line) |
| Dynasty | `7516` |
| Religion | `"norse_pagan"` |
| Culture | `norse` |
| Father | — (no `father=` line) |
| Mother | — (no `mother=` line) |
| DNA | — (absent) |
| Properties | — (absent) |

## Attributes (as in file)

| martial | diplomacy | intrigue | stewardship | learning |
|---|---|---|---|---|
| `9` | `9` | — | `6` | — |

## Traits (base entries, as in file)

- `ambitious`
- `cynical`
- `greedy`
- `gregarious`
- `grey_eminence`
- `brave`
- `heavy_infantry_leader`
- `genius`

## Other top-level fields (as in file)

- `give_nickname = nick_the_stranger`

## Bloodline(s) (as in file)

### `dyre` — granted 845.1.1

```text
		create_bloodline = {
			type = dyre
			has_dlc = "Holy Fury"
		}
```

## Family & relations (IDs as in file; names looked up from the same history files)

- Father: — (no `father=` line)
- Mother: — (no `mother=` line)
- Spouses:
  - added 863.1.1: `6872` — Sif (`characters/norse.txt`)
- Recorded children (1 other block(s) with `father=6811` or `mother=6811`):
  - `father=6811`: `6871` — Halfdan (`characters/norse.txt`), born 864.1.1

## Chronology (date order as in file; statements verbatim)

- **845.1.1** — `birth=yes; create_bloodline={ type = dyre has_dlc = "Holy Fury" }`
- **863.1.1** — `add_spouse=6872`
- **867.1.1** — `(see verbatim block below)`
- **882.1.1** — `death=yes`

## History entries (verbatim)

### 845.1.1

```text
	845.1.1={
		birth=yes
		create_bloodline = {
			type = dyre
			has_dlc = "Holy Fury"
		}
	}
```

### 863.1.1

```text
	863.1.1={
		add_spouse=6872
	}
```

### 867.1.1

```text
	867.1.1={
		wealth = 500
		effect = {
			spawn_unit = {
				province = 547
				owner = ROOT
				#leader = ROOT
				troops = {
					light_infantry = { 722 722 }
					heavy_infantry = { 156 156 }
					archers = { 200 200 }
				}
				attrition = 1.0
			}
			spawn_fleet = {
				province = closest # closest sea zone
				owner = ROOT
				cannot_inherit = yes # disbands on inheritance
				troops =
				{
					galleys = { 30 30 }
				}
			}
		}
	}
```

### 882.1.1

```text
	882.1.1={
		death=yes
	}
```

## Full character block (verbatim)

```text
6811 = {
	name="Dyre" # Ruler of Kiev
	dynasty=7516
	religion="norse_pagan"
	culture=norse
	martial=9
	diplomacy=9
	stewardship=6
	trait=ambitious
	trait=cynical
	trait=greedy
	trait=gregarious
	trait=grey_eminence
	trait=brave
	trait=heavy_infantry_leader
	trait=genius
#	trait=bastard
#	father = 163112
	give_nickname = nick_the_stranger # Oskyldr -> Askold
	845.1.1={
		birth=yes
		create_bloodline = {
			type = dyre
			has_dlc = "Holy Fury"
		}
	}
	863.1.1={
		add_spouse=6872
	}
	867.1.1={
		wealth = 500
		effect = {
			spawn_unit = {
				province = 547
				owner = ROOT
				#leader = ROOT
				troops = {
					light_infantry = { 722 722 }
					heavy_infantry = { 156 156 }
					archers = { 200 200 }
				}
				attrition = 1.0
			}
			spawn_fleet = {
				province = closest # closest sea zone
				owner = ROOT
				cannot_inherit = yes # disbands on inheritance
				troops =
				{
					galleys = { 30 30 }
				}
			}
		}
	}
	882.1.1={
		death=yes
	}
}
```
