# Árpád — character 159136

> Transcribed as-is from the history files. Nothing invented; values below are the exact tokens from the source block.
> Source: `characters/hungarian.txt` (line 9497), block `159136 = { ... }`.
> Display only: line endings normalized (CRLF → LF), encoding converted (Windows-1252 → UTF-8). Inline `#` comments are preserved in the verbatim sections.

## Identity (as in file)

| Field | Value in file |
|---|---|
| Character ID | `159136` |
| Name | `"Árpád"` |
| Sex | male (default — no `female=` line) |
| Dynasty | `708` |
| Religion | `"tengri_pagan"` |
| Culture | `"hungarian"` |
| Father | `159137` — Álmos (`characters/hungarian.txt`) |
| Mother | — (no `mother=` line) |
| DNA | — (absent) |
| Properties | — (absent) |

## Attributes (as in file)

| martial | diplomacy | intrigue | stewardship | learning |
|---|---|---|---|---|
| `8` | — | — | — | — |

## Traits (base entries, as in file)

- `ambitious`
- `strong`
- `just`
- `diligent`
- `brilliant_strategist`
- `flanker`

## Bloodline(s) (as in file)

### `arpad` — granted 868.1.1

```text
		create_bloodline = {
			type = arpad
			has_dlc = "Holy Fury"
		}
```

## Family & relations (IDs as in file; names looked up from the same history files)

- Father: `159137` — Álmos (`characters/hungarian.txt`)
- Mother: — (no `mother=` line)
- Spouses: — (no `add_spouse=` / `remove_spouse=` lines)
- Recorded children (5 other block(s) with `father=159136` or `mother=159136`):
  - `father=159136`: `159135` — Zolta (`characters/hungarian.txt`), born 880.1.2
  - `father=159136`: `163168` — Linütika (`characters/hungarian.txt`), born 861.1.1
  - `father=159136`: `163169` — Tarkatzus (`characters/hungarian.txt`), born 864.1.1
  - `father=159136`: `261311` — Jelek (`characters/hungarian.txt`), born 866.1.1
  - `father=159136`: `261314` — Jutotzas (`characters/hungarian.txt`), born 874.1.1

## Chronology (date order as in file; statements verbatim)

- **845.1.1** — `birth=yes`
- **868.1.1** — `create_bloodline={ type = arpad has_dlc = "Holy Fury" }`
- **907.1.1** — `death=yes`

## History entries (verbatim)

### 845.1.1

```text
	845.1.1={
		birth=yes
	}
```

### 868.1.1

```text
	868.1.1 = {
		create_bloodline = {
			type = arpad
			has_dlc = "Holy Fury"
		}
	}
```

### 907.1.1

```text
	907.1.1={
		death=yes
	}
```

## Full character block (verbatim)

```text
159136 = {
	name="Árpád"
	dynasty=708
	religion="tengri_pagan"
	culture="hungarian"
	father=159137
	martial=8
	trait=ambitious
	trait=strong
	trait=just
	trait=diligent
	trait=brilliant_strategist
	trait=flanker
	845.1.1={
		birth=yes
	}
	868.1.1 = {
		create_bloodline = {
			type = arpad
			has_dlc = "Holy Fury"
		}
	}
	907.1.1={
		death=yes
	}
}
```
