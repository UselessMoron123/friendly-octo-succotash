# Degna Mikael — character 145983

> Transcribed as-is from the history files. Nothing invented; values below are the exact tokens from the source block.
> Source: `characters/ethiopian.txt` (line 2833), block `145983 = { ... }`.
> Display only: line endings normalized (CRLF → LF), encoding converted (Windows-1252 → UTF-8). Inline `#` comments are preserved in the verbatim sections.

## Identity (as in file)

| Field | Value in file |
|---|---|
| Character ID | `145983` |
| Name | `"Degna Mikael"` |
| Sex | male (default — no `female=` line) |
| Dynasty | `7246` |
| Religion | `"miaphysite"` |
| Culture | `"ethiopian"` |
| Father | — (no `father=` line) |
| Mother | — (no `mother=` line) |
| DNA | — (absent) |
| Properties | — (absent) |

## Attributes (as in file)

| martial | diplomacy | intrigue | stewardship | learning |
|---|---|---|---|---|
| — | — | — | — | — |

## Traits (base entries, as in file)

_None at top level._

## Bloodline(s) (as in file)

### `solomon` — granted 620.1.1

```text
		create_bloodline = {
			type = solomon
			has_dlc = "Holy Fury"
		}
```

## Family & relations (IDs as in file; names looked up from the same history files)

- Father: — (no `father=` line)
- Mother: — (no `mother=` line)
- Spouses: — (no `add_spouse=` / `remove_spouse=` lines)
- Recorded children (1 other block(s) with `father=145983` or `mother=145983`):
  - `father=145983`: `145984` — Bahr Ikela (`characters/ethiopian.txt`), born 640.1.1

## Chronology (date order as in file; statements verbatim)

- **620.1.1** — `birth="620.1.1"; create_bloodline={ type = solomon has_dlc = "Holy Fury" }`
- **677.1.1** — `death="677.1.1"`

## History entries (verbatim)

### 620.1.1

```text
	620.1.1={
		birth="620.1.1"
		create_bloodline = {
			type = solomon
			has_dlc = "Holy Fury"
		}
	}
```

### 677.1.1

```text
	677.1.1={
		death="677.1.1"
	}
```

## Full character block (verbatim)

```text
145983 = {
	name="Degna Mikael"
	dynasty=7246
	religion="miaphysite"
	culture="ethiopian"

	620.1.1={
		birth="620.1.1"
		create_bloodline = {
			type = solomon
			has_dlc = "Holy Fury"
		}
	}
	677.1.1={
		death="677.1.1"
	}
}
```
