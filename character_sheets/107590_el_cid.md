# Rodrigo — character 107590

> Transcribed as-is from the history files. Nothing invented; values below are the exact tokens from the source block.
> Source: `characters/castillan.txt` (line 354), block `107590 = { ... }`.
> Display only: line endings normalized (CRLF → LF), encoding converted (Windows-1252 → UTF-8). Inline `#` comments are preserved in the verbatim sections.

## Identity (as in file)

| Field | Value in file |
|---|---|
| Character ID | `107590` |
| Name | `"Rodrigo"` |
| Sex | male (default — no `female=` line) |
| Dynasty | `407` |
| Religion | `"catholic"` |
| Culture | `"castillan"` |
| Father | `107600` — Diego (`characters/castillan.txt`) |
| Mother | `107601` — Teresa (`characters/castillan.txt`) |
| DNA | — (absent) |
| Properties | — (absent) |

## Attributes (as in file)

| martial | diplomacy | intrigue | stewardship | learning |
|---|---|---|---|---|
| `15` | `6` | `3` | `7` | — |

## Traits (base entries, as in file)

- `"diligent"`
- `"patient"`
- `"charitable"`
- `"brave"`
- `"brilliant_strategist"`

## Bloodline(s) (as in file)

### `el_cid` — granted 1043.1.1

```text
		create_bloodline = {
			type = el_cid
			has_dlc = "Holy Fury"
		}
```

## Family & relations (IDs as in file; names looked up from the same history files)

- Father: `107600` — Diego (`characters/castillan.txt`)
- Mother: `107601` — Teresa (`characters/castillan.txt`)
- Spouses:
  - added 1074.1.1: `108519` — Jimena (`characters/castillan.txt`)
- Recorded children (3 other block(s) with `father=107590` or `mother=107590`):
  - `father=107590`: `71541` — Diego (`characters/castillan.txt`), born 1076.1.1
  - `father=107590`: `71542` — Elvira (`characters/castillan.txt`), born 1080.1.1
  - `father=107590`: `210509` — María (`characters/castillan.txt`), born 1082.1.1

## Chronology (date order as in file; statements verbatim)

- **1043.1.1** — `birth="1043.1.1"; create_bloodline={ type = el_cid has_dlc = "Holy Fury" }`
- **1066.1.1** — `employer=107500`
- **1072.10.7** — `employer=108500`
- **1074.1.1** — `add_spouse=108519`
- **1082.1.1** — `employer=3935`
- **1086.1.1** — `employer=108500`
- **1088.1.1** — `employer=45005`
- **1094.1.1** — `employer=0`
- **1099.7.10** — `death="1099.7.10"`

## History entries (verbatim)

### 1043.1.1

```text
	1043.1.1={
		birth="1043.1.1"
		create_bloodline = {
			type = el_cid
			has_dlc = "Holy Fury"
		}
	}
```

### 1066.1.1

```text
	1066.1.1={
		employer=107500
	}
```

### 1072.10.7

```text
	1072.10.7={
		employer=108500
	}
```

### 1074.1.1

```text
	1074.1.1={
		add_spouse=108519
	}
```

### 1082.1.1

```text
	1082.1.1={
		employer=3935
	}
```

### 1086.1.1

```text
	1086.1.1={
		employer=108500
	}
```

### 1088.1.1

```text
	1088.1.1={
		employer=45005
	}
```

### 1094.1.1

```text
	1094.1.1={
		employer=0
	}
```

### 1099.7.10

```text
	1099.7.10={
		death="1099.7.10"
	}
```

## Full character block (verbatim)

```text
107590 = {
	name="Rodrigo" #AKA: El Cid
	dynasty=407
	martial=15
	diplomacy=6
	intrigue=3
	stewardship=7
	religion="catholic"
	culture="castillan"
	trait="diligent"
	trait="patient"
	trait="charitable"
	trait="brave"
	trait="brilliant_strategist"
	father=107600
	mother=107601
	1043.1.1={
		birth="1043.1.1"
		create_bloodline = {
			type = el_cid
			has_dlc = "Holy Fury"
		}
	}
	1066.1.1={
		employer=107500
	}
	1072.10.7={
		employer=108500
	}
	1074.1.1={
		add_spouse=108519
	}
	1082.1.1={
		employer=3935
	}
	1086.1.1={
		employer=108500
	}
	1088.1.1={
		employer=45005
	}
	1094.1.1={
		employer=0
	}
	1099.7.10={
		death="1099.7.10"
	}
}
```
