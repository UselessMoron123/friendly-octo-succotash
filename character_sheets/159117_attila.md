# Kubrat — character 159117

> Transcribed as-is from the history files. Nothing invented; values below are the exact tokens from the source block.
> Source: `characters/bolghar.txt` (line 661), block `159117 = { ... }`.
> Display only: line endings normalized (CRLF → LF), encoding converted (Windows-1252 → UTF-8). Inline `#` comments are preserved in the verbatim sections.

## Identity (as in file)

| Field | Value in file |
|---|---|
| Character ID | `159117` |
| Name | `"Kubrat"` |
| Sex | male (default — no `female=` line) |
| Dynasty | `591` |
| Religion | `"tengri_pagan"` |
| Culture | `"bolghar"` |
| Father | — (no `father=` line) |
| Mother | — (no `mother=` line) |
| DNA | — (absent) |
| Properties | — (absent) |

## Attributes (as in file)

| martial | diplomacy | intrigue | stewardship | learning |
|---|---|---|---|---|
| — | — | — | — | — |

## Traits (base entries, as in file)

_None at top level._

## Bloodline(s) (as in file)

### `attila` — granted 600.1.2

```text
		create_bloodline = {
			type = attila
			has_dlc = "Holy Fury"
		}
```

## Family & relations (IDs as in file; names looked up from the same history files)

- Father: — (no `father=` line)
- Mother: — (no `mother=` line)
- Spouses: — (no `add_spouse=` / `remove_spouse=` lines)
- Recorded children (3 other block(s) with `father=159117` or `mother=159117`):
  - `father=159117`: `159118` — Asparukh (`characters/bolghar.txt`), born 630.1.2
  - `father=159117`: `159119` — Kotrag (`characters/bolghar.txt`), born 631.1.2
  - `father=159117`: `159127` — Batbayan (`characters/bolghar.txt`), born 625.1.5

## Chronology (date order as in file; statements verbatim)

- **600.1.2** — `birth="600.1.2"; create_bloodline={ type = attila has_dlc = "Holy Fury" }`
- **665.1.2** — `death="665.1.2"`

## History entries (verbatim)

### 600.1.2

```text
	600.1.2={
		birth="600.1.2"
		create_bloodline = {
			type = attila
			has_dlc = "Holy Fury"
		}
	}
```

### 665.1.2

```text
	665.1.2={
		death="665.1.2"
	}
```

## Full character block (verbatim)

```text
159117 = {
	name="Kubrat"
	dynasty=591
	religion="tengri_pagan"
	culture="bolghar"
	600.1.2={
		birth="600.1.2"
		create_bloodline = {
			type = attila
			has_dlc = "Holy Fury"
		}
	}
	665.1.2={
		death="665.1.2"
	}
}
```
