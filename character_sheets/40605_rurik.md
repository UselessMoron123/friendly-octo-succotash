# Rurik — character 40605

> Transcribed as-is from the history files. Nothing invented; values below are the exact tokens from the source block.
> Source: `characters/norse.txt` (line 613), block `40605 = { ... }`.
> Display only: line endings normalized (CRLF → LF), encoding converted (Windows-1252 → UTF-8). Inline `#` comments are preserved in the verbatim sections.

## Identity (as in file)

| Field | Value in file |
|---|---|
| Character ID | `40605` |
| Name | `"Rurik"` |
| Sex | male (default — no `female=` line) |
| Dynasty | `705` |
| Religion | `"norse_pagan"` |
| Culture | `norse` |
| Father | — (no `father=` line) |
| Mother | — (no `mother=` line) |
| DNA | `tczeyfghtom` |
| Properties | `th0ebd000000` |

## Attributes (as in file)

| martial | diplomacy | intrigue | stewardship | learning |
|---|---|---|---|---|
| `8` | `12` | `6` | `8` | `3` |

## Traits (base entries, as in file)

- `gregarious`
- `ambitious`
- `just`
- `quick`
- `brilliant_strategist`
- `brave`

## Bloodline(s) (as in file)

### `rurik` — granted 830.1.1

```text
		create_bloodline = {
			type = rurik
			has_dlc = "Holy Fury"
		}
```

## Family & relations (IDs as in file; names looked up from the same history files)

- Father: — (no `father=` line)
- Mother: — (no `mother=` line)
- Spouses:
  - added 866.1.1: `6863` — Ingjerðr (`characters/norse.txt`)
- Recorded children (2 other block(s) with `father=40605` or `mother=40605`):
  - `father=40605`: `600` — Yngvar (`characters/norse.txt`), born 875.1.1
  - `father=40605`: `40606` — Helgi (`characters/norse.txt`), born 850.1.1

## Chronology (date order as in file; statements verbatim)

- **830.1.1** — `birth=yes; create_bloodline={ type = rurik has_dlc = "Holy Fury" }`
- **866.1.1** — `add_spouse=6863`
- **879.1.2** — `death=yes`

## History entries (verbatim)

### 830.1.1

```text
	830.1.1={
		birth=yes
		create_bloodline = {
			type = rurik
			has_dlc = "Holy Fury"
		}
	}
```

### 866.1.1

```text
	866.1.1={
		add_spouse = 6863
		wealth = 1000
		effect = {
			spawn_unit = {
				province = 414
				owner = ROOT
				#leader = ROOT
				troops = {
					light_infantry = { 507 507 }
					heavy_infantry = { 50 50 }
					archers = { 119 119 }
				}
				attrition = 1.0
			}
			spawn_fleet = {
				province = closest # closest sea zone
				owner = ROOT
				cannot_inherit = yes # disbands on inheritance
				troops =
				{
					galleys = { 40 40 }
				}
			}
		}
	}
```

### 879.1.2

```text
	879.1.2={
		death=yes
	}
```

## Full character block (verbatim)

```text
40605 = {
	name="Rurik" # Rurik, legendary founder of Rus. Probably known as Hrörekr by his Norse contemporaries.
	dynasty=705
	dna = tczeyfghtom
	properties = th0ebd000000
	religion="norse_pagan"
	culture=norse
	diplomacy = 12
	martial = 8
	stewardship = 8
	intrigue = 6
	learning = 3
	trait=gregarious
	trait=ambitious
	trait=just
	trait=quick
	trait=brilliant_strategist
	trait=brave
	830.1.1={
		birth=yes
		create_bloodline = {
			type = rurik
			has_dlc = "Holy Fury"
		}
	}
	866.1.1={
		add_spouse = 6863
		wealth = 1000
		effect = {
			spawn_unit = {
				province = 414
				owner = ROOT
				#leader = ROOT
				troops = {
					light_infantry = { 507 507 }
					heavy_infantry = { 50 50 }
					archers = { 119 119 }
				}
				attrition = 1.0
			}
			spawn_fleet = {
				province = closest # closest sea zone
				owner = ROOT
				cannot_inherit = yes # disbands on inheritance
				troops =
				{
					galleys = { 40 40 }
				}
			}
		}
	}
	879.1.2={
		death=yes
	}
}
```
