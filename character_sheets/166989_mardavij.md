# Mardavij — character 166989

> Transcribed as-is from the history files. Nothing invented; values below are the exact tokens from the source block.
> Source: `characters/persian.txt` (line 5848), block `166989 = { ... }`.
> Display only: line endings normalized (CRLF → LF), encoding converted (Windows-1252 → UTF-8). Inline `#` comments are preserved in the verbatim sections.

## Identity (as in file)

| Field | Value in file |
|---|---|
| Character ID | `166989` |
| Name | `"Mardavij"` |
| Sex | male (default — no `female=` line) |
| Dynasty | `101689` |
| Religion | `"zoroastrian"` |
| Culture | `"persian"` |
| Father | `166987` — Ziyar (`characters/persian.txt`) |
| Mother | `166988` — Mahtab (`characters/persian.txt`) |
| DNA | `bcedirmbfcy` |
| Properties | `be00ca00000` |

## Attributes (as in file)

| martial | diplomacy | intrigue | stewardship | learning |
|---|---|---|---|---|
| — | — | — | — | — |

## Traits (base entries, as in file)

- `"brilliant_strategist"`
- `"ambitious"`
- `"zealous"`
- `"wroth"`
- `"deceitful"`

## Bloodline(s) (as in file)

### `mardavij` — granted 930.1.1

```text
    	create_bloodline = {
    		type = mardavij
    		has_dlc = "Holy Fury"
    	}
```

## Family & relations (IDs as in file; names looked up from the same history files)

- Father: `166987` — Ziyar (`characters/persian.txt`)
- Mother: `166988` — Mahtab (`characters/persian.txt`)
- Spouses: — (no `add_spouse=` / `remove_spouse=` lines)
- Recorded children: — (no block has `father=166989` or `mother=166989`)

## Chronology (date order as in file; statements verbatim)

- **891.7.25** — `birth=yes`
- **930.1.1** — `create_bloodline={ type = mardavij has_dlc = "Holy Fury" }`
- **935.1.19** — `death=yes`

## History entries (verbatim)

### 891.7.25

```text
    891.7.25={
        birth=yes
    }
```

### 930.1.1

```text
    930.1.1 = {
    	create_bloodline = {
    		type = mardavij
    		has_dlc = "Holy Fury"
    	}
    }
```

### 935.1.19

```text
    935.1.19={
        death=yes
    }
```

## Full character block (verbatim)

```text
166989 = {
    name="Mardavij"
    dynasty=101689
    dna = bcedirmbfcy
    properties = be00ca00000
    religion="zoroastrian"
    culture="persian"
    father=166987
    mother=166988
    trait="brilliant_strategist"
    trait="ambitious"
    trait="zealous"
    trait="wroth"
    trait="deceitful"
    891.7.25={
        birth=yes
    }
    930.1.1 = {
    	create_bloodline = {
    		type = mardavij
    		has_dlc = "Holy Fury"
    	}
    }
    935.1.19={
        death=yes
    }
}
```
