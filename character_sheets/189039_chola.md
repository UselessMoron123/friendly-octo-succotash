# Irâcêntira — character 189039

> Transcribed as-is from the history files. Nothing invented; values below are the exact tokens from the source block.
> Source: `characters/tamil.txt` (line 146), block `189039 = { ... }`.
> Display only: line endings normalized (CRLF → LF), encoding converted (Windows-1252 → UTF-8). Inline `#` comments are preserved in the verbatim sections.

## Identity (as in file)

| Field | Value in file |
|---|---|
| Character ID | `189039` |
| Name | `"Irâcêntira"` |
| Sex | male (default — no `female=` line) |
| Dynasty | `1043008` |
| Religion | `"hindu"` |
| Culture | `"tamil"` |
| Father | `189038` — Arulmolivarman (`characters/tamil.txt`) |
| Mother | — (no `mother=` line) |
| DNA | — (absent) |
| Properties | — (absent) |

## Attributes (as in file)

| martial | diplomacy | intrigue | stewardship | learning |
|---|---|---|---|---|
| — | — | — | — | — |

## Traits (base entries, as in file)

- `kshatriya`
- `shaivist_hindu`

## Bloodline(s) (as in file)

### `chola` — granted 1040.1.1

```text
		create_bloodline = {
			type = chola
			has_dlc = "Holy Fury"
		}
```

## Family & relations (IDs as in file; names looked up from the same history files)

- Father: `189038` — Arulmolivarman (`characters/tamil.txt`)
- Mother: — (no `mother=` line)
- Spouses: — (no `add_spouse=` / `remove_spouse=` lines)
- Recorded children (3 other block(s) with `father=189039` or `mother=189039`):
  - `father=189039`: `189040` — Irâcâtirâca (`characters/tamil.txt`), born 1008.1.1
  - `father=189039`: `189041` — Irâcêntira (`characters/tamil.txt`), born 1009.1.1
  - `father=189039`: `189042` — Vîrarâcêntira (`characters/tamil.txt`), born 1010.1.1

## Chronology (date order as in file; statements verbatim)

- **980.1.1** — `birth="980.1.1"`
- **1040.1.1** — `give_nickname="nick_the_great"; create_bloodline={ type = chola has_dlc = "Holy Fury" }`
- **1044.1.1** — `death="1044.1.1"`

## History entries (verbatim)

### 980.1.1

```text
	980.1.1={
		birth="980.1.1"
	}
```

### 1040.1.1

```text
	1040.1.1={
		give_nickname="nick_the_great"
		create_bloodline = {
			type = chola
			has_dlc = "Holy Fury"
		}
	}
```

### 1044.1.1

```text
	1044.1.1={
		death="1044.1.1"
	}
```

## Full character block (verbatim)

```text
189039 = {
	name="Irâcêntira" #Rajendra
	dynasty=1043008
	religion="hindu"
	culture="tamil"
	trait=kshatriya
	trait=shaivist_hindu
	father=189038
	980.1.1={
		birth="980.1.1"
	}
	1040.1.1={
		give_nickname="nick_the_great"
		create_bloodline = {
			type = chola
			has_dlc = "Holy Fury"
		}
	}
	1044.1.1={
		death="1044.1.1"
	}
}
```
