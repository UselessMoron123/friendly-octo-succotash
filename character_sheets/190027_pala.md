# Gopala — character 190027

> Transcribed as-is from the history files. Nothing invented; values below are the exact tokens from the source block.
> Source: `characters/bengali.txt` (line 31), block `190027 = { ... }`.
> Display only: line endings normalized (CRLF → LF), encoding converted (Windows-1252 → UTF-8). Inline `#` comments are preserved in the verbatim sections.

## Identity (as in file)

| Field | Value in file |
|---|---|
| Character ID | `190027` |
| Name | `"Gopala"` |
| Sex | male (default — no `female=` line) |
| Dynasty | `1043000` |
| Religion | `"buddhist"` |
| Culture | `"bengali"` |
| Father | `190026` — Vapyata (`characters/bengali.txt`) |
| Mother | — (no `mother=` line) |
| DNA | — (absent) |
| Properties | — (absent) |

## Attributes (as in file)

| martial | diplomacy | intrigue | stewardship | learning |
|---|---|---|---|---|
| — | — | — | — | — |

## Traits (base entries, as in file)

- `"kshatriya"`
- `"mahayana_buddhist"`

## Bloodline(s) (as in file)

### `pala` — granted 734.1.1

```text
		create_bloodline = {
			type = pala
			has_dlc = "Holy Fury"
		}
```

## Family & relations (IDs as in file; names looked up from the same history files)

- Father: `190026` — Vapyata (`characters/bengali.txt`)
- Mother: — (no `mother=` line)
- Spouses:
  - added 750.1.1: `191301` — Dedadevi (`characters/bengali.txt`)
- Recorded children (2 other block(s) with `father=190027` or `mother=190027`):
  - `father=190027`: `190006` — Vakapala (`characters/bengali.txt`), born 757.1.1
  - `father=190027`: `190028` — Dharmapala (`characters/bengali.txt`), born 753.1.1

## Chronology (date order as in file; statements verbatim)

- **734.1.1** — `birth=yes; create_bloodline={ type = pala has_dlc = "Holy Fury" }`
- **750.1.1** — `add_spouse=191301`
- **769.1.1** — `death=yes`

## History entries (verbatim)

### 734.1.1

```text
	734.1.1={
		birth=yes
		create_bloodline = {
			type = pala
			has_dlc = "Holy Fury"
		}
	}
```

### 750.1.1

```text
	750.1.1={
		add_spouse=191301
	}
```

### 769.1.1

```text
	769.1.1={
		death=yes
	}
```

## Full character block (verbatim)

```text
190027 = {
	name="Gopala" 
	dynasty=1043000
	religion="buddhist"
	culture="bengali"
	father=190026
	trait="kshatriya"
	trait="mahayana_buddhist"
	734.1.1={
		birth=yes
		create_bloodline = {
			type = pala
			has_dlc = "Holy Fury"
		}
	}
	750.1.1={
		add_spouse=191301
	}
	769.1.1={
		death=yes
	}
}
```
